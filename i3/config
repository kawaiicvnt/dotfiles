# xrandr settings:	UW: 1920x645 pos, TV: 0x645 pos, PORT: 4480x0 pos
exec --no-startup-id xrandr --dpi 150 --output eDP-1 
bindsym $mod+Ctrl+Shift+D exec xrandr --output eDP-1 --pos 3840x360 --output DP-1 --auto --pos 0x0
bindsym $mod+Ctrl+Shift+P exec xrandr -s 2880x1800 --output eDP-1 --pos 0x0
# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#


# DEPENDANCIES:
# Menu:			dmenu
# Bar:			i3blocks, polybar
# Lock:			i3lock
# Background:		feh
# Console:		konsole, kitty
# Transparency:		compton
# Volume:		alsa, alsa-utils
# Playback:		smart-playerctl (AUR)
# Autostart:		discord (AUR), spotify (AUR)
# File Manager:		thunar, xarchiver, gvfs, gvfs-smb, sshfs, thunar-archive-plugin, tumbler
# Cursor Accel:		xf86-input-libinput
# Screenshots:		scrot
# Notifications:	dunst 

# NOTES
# xrandr settings:	UW: 1920x645 pos, TV: 0x645 pos, PORT: 4480x0 pos

##### COLORS #####
#set $bg-color            #2f343f
#set $inactive-bg-color   #2f343f
#set $text-color          #f3f4f5
#set $inactive-text-color #676E7D
#set $urgent-bg-color     #E53935

# dbus warning with thunar
exec --no-startup-id export NO_AT_BRIDGE=1

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

# alt key
#set $mod Mod1
# super key
set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:feather 11

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet

# OLD AUDIO SETTINGS #
# Use pactl to adjust volume in PulseAudio.
# set $refresh_i3status killall -SIGUSR1 i3status
# bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
# bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
# bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
# bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status


# WINDOW MANAGEMENT #

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Shift+Return exec konsole
bindsym $mod+Return exec kitty

# kill focused window
bindsym $mod+Shift+q kill


# DMENU #

# start dmenu (a program launcher)
bindsym $mod+Shift+d exec --no-startup-id dmenu_run -i -nb '#58002c' -sb '#7f002c'
#bindsym $mod+d exec --no-startup-id i3-dmenu-desktop --dmenu="dmenu -i -nb '#58002c' -sb '#7f002c'"
bindsym $mod+d exec --no-startup-id ~/.config/polybar/blocks/scripts/launcher.sh
# -i makes it case-insensitive

# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

set $smallstep 15px
set $bigstep 100px

# Bigger step
# move focused window
bindsym $mod+Shift+j move left $bigstep
bindsym $mod+Shift+k move down $bigstep
bindsym $mod+Shift+l move up $bigstep
bindsym $mod+Shift+semicolon move right $bigstep

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left $bigstep
bindsym $mod+Shift+Down move down $bigstep
bindsym $mod+Shift+Up move up $bigstep
bindsym $mod+Shift+Right move right $bigstep

# Smaller step
# move focused window
bindsym $mod+Ctrl+Shift+j move left $smallstep
bindsym $mod+Ctrl+Shift+k move down $smallstep
bindsym $mod+Ctrl+Shift+l move up $smallstep
bindsym $mod+Ctrl+Shift+semicolon move right $smallstep

# alternatively, you can use the cursor keys:
bindsym $mod+Ctrl+Shift+Left move left $smallstep
bindsym $mod+Ctrl+Shift+Down move down $smallstep
bindsym $mod+Ctrl+Shift+Up move up $smallstep
bindsym $mod+Ctrl+Shift+Right move right $smallstep


# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+Tab floating toggle

# change focus between tiling / floating windows
bindsym $mod+Tab focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child


# WORKSPACES #

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"
#set $ws11 "-"
#set $ws12 "+"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10
#bindsym $mod+minus workspace number $ws11
#bindsym $mod+= workspace number $ws12

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10
#bindsym $mod+Shift+- move container to workspace number $ws11
#bindsym $mod+Shift+= move container to workspace number $ws12



# PASSTHROUGH MODE #
mode  "Passthrough" {
	bindsym XF86AudioRaiseVolume 	exec --no-startup-id amixer sset Master 2%+
	bindsym XF86AudioLowerVolume 	exec --no-startup-id amixer sset Master unmute
	bindsym XF86AudioMute 		exec --no-startup-id amixer sset Master toggle
	bindsym 248 			exec --no-startup-id amixer sset Capture toggle
	bindsym XF86AudioPlay 		exec --no-startup-id smart-playerctl play-pause
	bindsym XF86AudioPause 		exec --no-startup-id smart-playerctl play-pause
	bindsym XF86AudioNext 		exec --no-startup-id smart-playerctl next
	bindsym XF86AudioPrev 		exec --no-startup-id smart-playerctl previous
	bindsym XF86MonBrightnessUp 	exec --no-startup-id brightnessctl s 10%+
	bindsym XF86MonBrightnessDown 	exec --no-startup-id brightnessctl s 10%-
	bindsym $mod+Shift+Return 	exec konsole
	bindsym $mod+Return 		exec kitty
        bindsym $mod+F12 		mode "default"
}
bindsym $mod+F12 mode "Passthrough"

# SCRATCHPAD #

# Make the currently focused window a scratchpad
bindsym $mod+Shift+minus move scratchpad

# Show the first scratchpad window
bindsym $mod+Shift+plus scratchpad show



# AUDIO CONTROLS #

#bindsym XF86AudioRaiseVolume 	exec --no-startup-id pactl set-sink-volume 2 +2% #increase sound volume
bindsym XF86AudioRaiseVolume 	exec --no-startup-id amixer sset Master 2%+
#bindsym XF86AudioLowerVolume 	exec --no-startup-id pactl set-sink-volume 2 -2% #decrease sound volume
bindsym XF86AudioLowerVolume 	exec --no-startup-id amixer sset Master unmute 2%-
#bindsym XF86AudioMute 		exec --no-startup-id pactl set-sink-mute 2 toggle # mute sound
bindsym XF86AudioMute 		exec --no-startup-id amixer sset Master toggle
bindsym 248 			exec --no-startup-id amixer sset Capture toggle
bindsym XF86AudioPlay 		exec smart-playerctl play-pause
bindsym XF86AudioPause 		exec smart-playerctl play-pause
bindsym XF86AudioNext 		exec smart-playerctl next
bindsym XF86AudioPrev 		exec smart-playerctl previous
bindsym XF86MonBrightnessUp 	exec --no-startup-id brightnessctl s 10%+
bindsym XF86MonBrightnessDown 	exec --no-startup-id brightnessctl s 10%-

# screenshot
bindsym --release $mod+Shift+p exec scrot --select $HOME/Pictures/screenshots/`date +%Y-%m-%d_%H:%M:%S`.png
#FIND KEYSYMS:   xev -event keyboard  | egrep -o 'keycode.*\)'

# XRANDR #
#exec --no-startup-id xrandr --dpi 60 --output DVI-D-0 --off --output HDMI-0 --primary --mode 2560x1080 --rate 75 --pos 1920x645 --rotate normal --output DP-0 --off --output DP-1 --mode 1920x1080 --pos 0x645 --rotate normal --output HDMI-1-1 --mode 1920x1080 --rate 50 --pos 4480x0 --rotate left --output DVI-I-1-1 --off --output VGA-1-1 --off

# i3 SESSION #

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# exit i3 (logs you out of your X session)
bindsym $mod+Ctrl+Shift+e exec "i3-nagbar -t warning -m 'Oya? You leavin?' -b 'Haii' 'i3-msg exit'"
bindsym $mod+Shift+e exec "~/.config/polybar/blocks/scripts/powermenu.sh"

# lockscreen (i3-lock)
bindsym $mod+Shift+x exec "~/.config/i3/i3lock.sh"


# resize window (you can also use the mouse for that)
mode "Resize" {
        
	set $big_step 100
	set $small_step 10
	set $micro_step 2

	# These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        
	# Bigger step
	bindsym j resize shrink width $big_step px or $big_step ppt
        bindsym k resize grow height $big_step px or $big_step ppt
        bindsym l resize shrink height $big_step px or $big_step ppt
        bindsym semicolon resize grow width $big_step px or $big_step ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width $big_step px or $big_step ppt
        bindsym Down resize grow height $big_step px or $big_step ppt
        bindsym Up resize shrink height $big_step px or $big_step ppt
        bindsym Right resize grow width $big_step px or $big_step ppt

	# Smaller step
        # Pressing down will grow the window’s height.
        bindsym Ctrl+j resize shrink width $small_step px or $small_step ppt
        bindsym Ctrl+k resize grow height $small_step px or $small_step ppt
        bindsym Ctrl+l resize shrink height $small_step px or $small_step ppt
        bindsym Ctrl+semicolon resize grow width $small_step px or $small_step ppt

        # same bindings, but for the arrow keys
        bindsym Ctrl+Left resize shrink width $small_step px or $small_step ppt
        bindsym Ctrl+Down resize grow height $small_step px or $small_step ppt
        bindsym Ctrl+Up resize shrink height $small_step px or $small_step ppt
        bindsym Ctrl+Right resize grow width $small_step px or $small_step ppt

	# Micro step
        # Pressing down will grow the window’s height.
        bindsym Shift+j resize shrink width $micro_step px or $micro_step ppt
        bindsym Shift+k resize grow height $micro_step px or $micro_step ppt
        bindsym Shift+l resize shrink height $micro_step px or $micro_step ppt
        bindsym Shift+semicolon resize grow width $micro_step px or $micro_step ppt

        # same bindings, but for the arrow keys
        bindsym Shift+Left resize shrink width $micro_step px or $micro_step ppt
        bindsym Shift+Down resize grow height $micro_step px or $micro_step ppt
        bindsym Shift+Up resize shrink height $micro_step px or $micro_step ppt
	bindsym Shift+Right resize grow width $micro_step px or $micro_step ppt


	# back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "Resize"


##### COLORS #####
set $bg-color            #2f343f
set $inactive-bg-color   #2f343f
set $text-color          #f3f4f5
set $inactive-text-color #676E7D
set $urgent-bg-color     #E53935


# window colors
#                       border              background         text                 indicator
client.focused          $bg-color           $bg-color          $text-color          #00ff00
client.unfocused        $inactive-bg-color  $inactive-bg-color $inactive-text-color #00ff00
client.focused_inactive $inactive-bg-color  $inactive-bg-color $inactive-text-color #00ff00
client.urgent           $urgent-bg-color    $urgent-bg-color   $text-color          #00ff00


# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
#bar {
#	position top
#        status_command i3blocks -c ~/.config/i3/i3blocks.conf
#	colors {
#		background #0a020a
#		separator #757575
#		#                  border             background         text
#		focused_workspace  #0b223b            #0b222b            $text-color
#		inactive_workspace #0a131a            #0a131a            $inactive-text-color
#		urgent_workspace   $urgent-bg-color   $urgent-bg-color   $text-color
#	}
#}

## Polybar
#start polybar
#exec_always --no-startup-id ~/.config/i3/polybar.sh &

# If polybar freezes
#bindsym $mod+Shift+t exec --no-startup-id "bash killall polybar ; bash $HOME/.config/i3/polybarone.sh"
bindsym $mod+Shift+t exec --no-startup-id "polybar --reload main -c $HOME/.config/polybar/blocks/config.ini &"

#make polybar start in both screens
exec_always --no-startup-id "bash $HOME/.config/i3/polybarone.sh"

##### XRANDR - MONITOR SETUP #####
#exec --no-startup-id xrandr --output DVI-D-0 --off --output HDMI-0 --primary --mode 2560x1080 --rate 75 --pos 1920x645 --rotate normal --output DP-0 --off --output HDMI-1-1 --mode 1920x1080 --pos 0x645 --rotate normal --output DP-1 --mode 1920x1080 --rate 50 --pos 4480x0 --rotate left --output DVI-I-1-1 --off --output VGA-1-1 --off
set $firstMonitor HDMI-0
set $secondMonitor DP-1
set $thirdMonitor HDMI-1-1

##### FEH - WALLPAPER #####
# I think it goes with xrandr monitor order
exec --no-startup-id feh --bg-scale /home/shared/Pictures/wallpapers/kyubey.jpg
#  /home/shared/Pictures/hutao3.png /home/shared/Pictures/hutao1.png

##### COMPI - TRANSPARENCY #####
exec --no-startup-id picom -bcCG

##### MOUSE ACCEL #####
# Change the number after the --set-prop with your mouse id (find it using "xinput list")
#exec --no-startup-id xinput --set-prop 18 'libinput Accel Profile Enabled' 0, 1
exec --no-startup-id "bash $HOME/.config/i3/xinput.sh"

##### WORKSPACE - MONITOR SETTINGS #####
workspace $ws1 output $firstMonitor
workspace $ws2 output $firstMonitor
workspace $ws3 output $firstMonitor
workspace $ws4 output $firstMonitor
workspace $ws5 output $firstMonitor
workspace $ws6 output $firstMonitor
workspace $ws7 output $secondMonitor
workspace $ws8 output $secondMonitor
workspace $ws9 output $thirdMonitor
workspace $ws10 output $thirdMonitor

# assign default workspaces
assign [class="Spotify"] $ws7
for_window [class="Spotify"] move to scratchpad
assign [class="discord"] $ws7
assign [class="*virtualbox*"] $ws6
#for_window [class="*virtualbo*"] move to workspace $ws6
assign [class="Minecraft"] $ws2
for_window [class="Minecraft"] move to workspace $ws2
# floating windows
for_window [instance="nomacs"] floating enable
for_window [instance="galculator"] floating enable

# start default apps probably needed
exec --no-startup-id spotify
#exec --no-startup-id discord

# gaps
for_window [class=".*"] border pixel 0
#bindsym $mod+g 
gaps top 34
gaps inner 5
gaps right 5
#gaps left 5
gaps bottom 5
for_window [class="firefox"] gaps top all 35

#smart_gaps on
#smart_gaps inverse_outer

#smart_borders on
#smart_borders no_gaps

# change language
# bindsym $mod+space exec (setxkbmap -query | grep -q "layout:\s\+us") && xkb-switch -s gr || xkb-switch -s us
exec --no-startup-id "setxkbmap -model pc105 -layout us,gr -option grp:win_space_toggle"
#exec --no-startup-id setxkbmap -model pc105 -layout us,gr -option grp:alt_shift_toggle
bindsym $mod+Shift+i exec --no-startup-id "setxkbmap -model pc105 -layout us,gr -option grp:win_space_toggle"

# BLUETOOTH #
# Anne Pro 2
exec --no-startup-id echo -e "power on\nagent on\nconnect 64:33:DB:AD:F6:A9" | bluetoothctl

# NOTIFICATION SERVER #
exec --no-startup-id dunst
